name: Build Windows (Tauri v2)

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
      - name: Install dependencies
        run: npm ci || true
      - name: Build (Windows)
        run: |
          echo "Skeleton build step for Windows (Tauri v2) - replace with real build commands."
